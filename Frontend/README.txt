To run the app you should:

- Install the latest version of node.js (available at https://nodejs.org/en/download/package-manager)
- Test node with npm -v (It should show the version installed)
- Install ionic with npm install -g @ionic/cli
- Enter the project folder using cd folder-name
- Install dependencies with npm i
- Run using ionic serve